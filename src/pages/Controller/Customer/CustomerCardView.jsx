import React from "react";
import UserCard from "../../../components/Card/UserCard/UserCard";

const CustomerCardView = ({ data }) => {
  return <UserCard data={data} />;
};

export default CustomerCardView;
